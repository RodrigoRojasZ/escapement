# Entorno / build
.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Secretos y config local
.env
.env.*
!.env.example
escapement.toml

# Lista de términos vetados del guard de publicación: no se publica (ver el .example)
.publicacion-veto.txt

# Test / cache
.pytest_cache/
.ruff_cache/

# Modelos / audio locales (fases de voz y LLM local)
models/
*.gguf
*.onnx
*.wav

# Datos operacionales locales (ledger, cola, conversaciones, sesión de voz) — no subir a GitHub
data/
